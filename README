kpi_adder

Description:
  The host sends each element of an int array to the kernel.
  The kernel adds the received element and returns the cumulative total

Expected output:
  For each element:
    printf("%i -- Sent = %i -- Total = %i\n", i, data[i], total);
  When finished:
    printf("Done!\n");

Build and run:
  1) Provide this git URL into KWS and click 'Run'.
